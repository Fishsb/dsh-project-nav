# project-nav 架构（v0.12.2 · 在场的治理层）

> **上位约束**：所有开发动作必须从架构出发。动手前能说出这次改动落在哪个架构节点；说不出 = 还没做架构思考，十有八九是局部补丁。
>
> 本文是**架构契约**，不是说明文档。改架构先改它。

---

## 1. 一句话架构

> **一条 append-only 事件流（唯一事实源）→ 一个由它折叠出的架构模型（可丢弃缓存）→ 一层按需产出（零落盘）。**
>
> **模型 = 包含树（谁属于谁）+ 依赖图（谁引用谁）。**
> **依赖图由磁盘 import 派生——不登记、不手写、不会过期。**
>
> **闸门是对模型的查询；产出有两种形态，都只服务 agent：`nav_graph` 按需直出，与在场层注入上下文。**
> **治理面不落任何文件**（0.12.0 换代：落盘投影整体退场）。

## 2. 治理面最小化（第一性判据）

> **会因代码变更而"过期"的文档，不该是文档 —— 该是投影。**

投影 = 零手写、随时重生、**没有"过期"这回事**（因为没人维护它）。于是手写面只剩一样：

| 面 | 载体 | 何时改 |
|---|---|---|
| **契约** | `ARCHITECTURE.md` · `AGENTS.md` | 只在架构换代 / 纪律变更时 |
| **产出** | `nav_graph` 直出 · **在场层**注入（_不物化成文件_） | 永不手写，随时重算 |

⚠ **0.12.0 换代（ADR-268）**：投影的"物化"整体退场 —— `PROJECT.md` 标记区 / `.internal/ARCH-MODEL.md` /
地图 HTML 三个**落盘**投影已删除。判因：它们的**唯一读者是人**，而定案是**治理面只服务 agent**；
实测全仓零机器读者，且 `ARCH-MODEL.md` 自述的唯一理由「进仓以便 `git diff` 审查」在治理根
**不成立**（那不是 git 仓库）。其 agent 形态的替代物是**在场层**（§7.1）。

**推论**：新增任何"要维护"的文档前，先回答它为什么不是投影；答不上来就让它变成投影，或不要它。
**信息可达性（治理开销的收敛方式，0.10.5）**：省 token 只能靠**压缩去冗余**，不能靠**截断丢信息**。三条纪律：
① 零信息重复必删（同一教义/清单在描述、输出、契约多处复述 ⇒ 留一处权威，其余删除或改为引用）；
② 任何"少展示"必须**可见且可取回**（带 `共N/+N` 交代与取回路径；条目一条不丢的全量索引优于截前 N 条）；
③ 确定性渲染 —— 同一模型 ⇒ 同一字节（投影不含时间戳等第二输入；模型不变则重渲染零 diff）。
**刻意不设字节上限**：上限会逼着无声削内容，正是 ①② 的反面；体积只自报，是信号不是裁决。
⚠ 在场层的输出**按每轮计费**，故"常驻摘要必须短"是这条判据的**新形态**：它的省略同样一律带
`共N` + 取回路径（`nav_graph mode=health`）。

`.internal/arch/*.md` 曾是要维护的资产（sha1 指纹 + 新鲜度机检 + 行号重锚），现降级为**按需临时投影**：
用完即弃，不登记、不盖指纹、不进健康检查。要让它被按任务检索到，登记成普通 `artifact` 节点
（`nav_node layer=artifact when=…`），走 `nav_graph mode=docs`。

**响应性判据（0.11.0 · 治理开销的另一半）**：治理不只是"说了多少"，还有"每次说之前算了多少"。
本插件的每个工具调用都重建模型，而模型含**磁盘实况**（走文件 + import 扫描）——
治理根实测 `walkFiles` 52ms + `scanImports` 360ms ≈ **412ms/次**，而每次调用都白付。

判据：**纯磁盘派生的重活必须可缓存，且以磁盘指纹自证新鲜度**。
落地为 `runtime/scan-cache.json`（`scanImportsCached`），指纹 = 文件清单 + 每个被扫文件的 `size/mtimeMs`。
实测 `loadModel` 193ms → 70ms（**省 123ms / 64%**）。

⚠ **缓存有效性不得用耗时判定**：耗时是代理指标，机器一忙就漂。机检判据取 **`fromCache` 布尔**
（命中/失效是可确定断言的量）。这一条与 §10「不设行数红线」同根：**结构可断言，度量只能参考**。

**在场层的响应性（0.12.0）**：在场层的 text provider **每轮组装都要跑**，故它**不得**走磁盘扫描路径。
判据：**组装期只允许纯事件流折叠**（`foldOnly`，实测 5.1ms），**禁止** `buildModel`/`loadModel`
（实测 177ms，含 `walkFiles`+`scanImports`）。两者相差 **35×**；这是"每轮计费"特有的约束
—— 工具调用可以慢，因为它是用户主动发起的；每轮组装不能慢，因为它是**每个请求**都要付的。
⚠ 与上面的缓存判据同源：**能确定断言的量优先**（"有没有调 buildModel"可机检，而非"快不快"）。

## 3. 两层数据面（PLANE · 0.12.0 由 3 层收敛）

| 层 | 路径 | 生命周期 | 版本控制 | 谁能写 |
|---|---|---|---|---|
| **事件流** | `.internal/events.jsonl` | 永久 | **是** | 只追加，永不修改（唯一真相） |
| **运行时** | `.internal/runtime/` | 短命 | 否 | 模型缓存 · 在途意图 · 锁 · 诊断（**含依赖图**）· **扫描缓存** |

**不存在第三层**：任何新的长期状态先回答"它是事件，还是渲染？"，两者都不是就不该存在。
**原第三层「渲染投影」已整体退场**（0.12.0）：产出不再物化 ⇒ 没有第三种文件形态。
**依赖图不是第三层**——它属于第二层的「磁盘实况」，与 STALE 探测、缺口扫描在同一位置计算。
**扫描缓存（0.11.0）也不是新层**：它是第二层里**同一份磁盘实况的加速器**（`runtime/scan-cache.json`），
指纹 = 文件清单 + 每个被扫文件的 `size/mtimeMs`。任何一项变化即整份作废——
缓存自证新鲜度的依据是**磁盘**，不是事件流戳，故它仍只是"磁盘实况"，不是第二个真相（I1）。
**在场层（0.12.0）更不是新层**：它**零状态**——每次组装现算（`foldOnly`），不缓存、不落盘。
判据是结构性的：**能被丢掉的那份不可能是真相**（I1），而在场层连"能被丢掉的那份"都没有。

## 4. 三条不变式（可机检）

- **I1 单源**：模型每个属性都能由 `事件流 + 磁盘实况` 复算。缓存无法自证与源一致时，它不是缓存，是第二个真相。
  在场层同样零新增状态：它只消费事件流派生量，**不记录任何事实**。
- **I2 产出**：一切机器可读产出都是对模型的**纯函数派生**（`nav_graph` 直出 / 在场层注入）。
  **治理面零落盘** ⇒ 验法从"手改渲染物会被覆盖"变成**"调用渲染后治理根无任何新文件"**（更强的判据）。
- **I3 可丢弃**：**删除 `runtime/` → 治理零损失**，下次调用重建（依赖图同办）。在场层不写 `runtime/`。

## 5. 事件模型（4 种 kind，唯一写入面）

| kind | 语义 |
|---|---|
| `commit` | 一次改动意图（anchor + scope + `arch=`）；`open` 记 scope 证据，`closed` 记收口结果 |
| `decide` | 架构决策（ADR）；`id = ADR-<seq>` |
| `node` | 节点 upsert / 退役（级联） |
| `set` | 主线向量 doing / next / notDoing / exitCondition |

`seq` 由追加顺序分配、ID 由 seq 派生 ⇒ 并发追加不可能撞 ID。
**收口不依赖会话**：`open` 的 scope 证据 = 文件 `{size, mtimeMs, sha1}`；证据已变 ⇒ 下次任意工具调用自动收口；未变 ⇒ 意图继续在途（**在途 = 有人正在改，不是孤儿**）。比对**先判存在性、再比内容**——先比 sha1 会把"消失"误报成"被修改"。

⚠ **折叠语义（实测，0.12.0 澄清）**：收口时**原笔被就地标 `closed` 并挂 `closes` = 自己的 seq**；
收口回执是**另一条 commit**（`phase=closed`，无 `closes`，`plan` 结构性为空串）。
故取"原始申报"应直取原笔的 `plan` —— **不存在**"回查 `closes` 指向的笔"这种操作
（`closes` 只写在原笔自己身上；实测治理根 21 条带 `closes` 的记录中 `closes !== seq` 者 **0 条**）。
（旧版 `renderModelDoc` 写过同名回查，实为 no-op：值对、注释错。）

## 6. 七个闸门（全部是 `nav_commit` 内的模型查询）

| 闸门 | 判据 | 强度 |
|---|---|---|
| **锚点闸** | 架构节点真实存在（或锚定 `.internal/arch/*.md`） | 拒 |
| **范围闸** | 撞 `notDoing`？撞他人在途 scope？ | 拒 / 告警 |
| **主线闸** | scope 里的模块被 `doing/next` 引用？ | 告警 |
| **计数闸** | 同一锚点自上次决策以来**补丁数** ≥ 3？（补丁 = `open` 意图 + 无配对的 `closed` 记录；**收口回执不重复计数** —— 否则每笔改动被计两次，阈值 3 实际在 ~1.5 笔就触发） | 拒 |
| **决策闸** | `arch=` 缺失？ | 告警 |
| **影响面闸** | scope 内节点的**下游**（谁引用我）未纳入 scope？ | 告警 |
| **完结闸** | 有该收而未收的意图？ | 自动 + 报异常 |

## 7. 工具面（5 个，每个 = 模型上的一种操作）

| 工具 | 操作 |
|---|---|
| `nav_graph` | **读**：落点 / 影响面 / 缺口 / 覆盖度 / 文档路由 / 健康 / 决策 / 地图 / **json 全量** |
| `nav_commit` | **写**：登记改动意图（锚点 + scope + `arch=`）；开新笔时按证据自动收口 |
| `nav_decide` | **写**：架构决策（挂节点；登记即重置该节点补丁计数） |
| `nav_node` | **写**：节点 upsert / 退役并级联；文档工件注册同此入口 |
| `nav_set` | **写**：主线向量 |

工具数下降不是目标，是"闸门变查询、产出变在场"的结果。
⚠ **`nav_render` 已随落盘投影退场（0.12.0）**：它唯一的操作是"重生成落盘投影"，落盘面消失后它无事可做。
留一个空工具比删掉更坏 —— **它会让工具面说谎**。

### 7.1 在场层（0.12.0 · 落盘投影的 agent 形态替代物）

**病根**（0.11.0 实测自述）：七闸只在 `nav_commit` 内跑 ⇒ **不调用它 = 完全绕行无痕迹**，
治理根登记率仅 **3%**（4747 文件 / 4600 未登记）。根因不是闸门不够，而是**治理只在被调用时存在**。

落盘投影曾是"给人读"的产物。只服务 agent 之后，正确的替代物不是另一个文件，而是**把治理放进 agent 的上下文**。
⚠ **只有一条注入路径**（0.12.0 修正，ADR-276）：

| 出口 | 机制 | 触发 |
|---|---|---|
| **常驻摘要** | `ctx.systemPrompt.section({ text: (c) => … })` | **每轮组装求值**（`text` 必须是函数） |

**三条硬约束**：
1. **走廉价路径**：只吃纯事件流折叠（`foldOnly`，实测 **5.1ms**），**禁止**组装期 `buildModel`（177ms）。
2. **零写盘**：不落 `runtime/`、不建锁、不追加事件 ⇒ I1/I3 完好。**任何"先落盘再注入"的实现都是回退。**
3. **不知道的不说**：磁盘实况（STALE / 缺口 / 依赖图）不在廉价模型里，故摘要**不报**它们
   —— 报一个没算过的数就是假绿。

**⚠ 为什么没有第二条路径（`tools/post-execute` + `additionalContexts`）** —— 0.12.0 初版曾有，**已删除**：
- **它是纯重复**：`text` **每轮组装即求值**，故每次请求的系统提示里已是最新治理；再在每次写类工具后
  注入**同一份**文本 ⇒ 零新增信息。实测（真实会话日志）：`system/message` 每轮 1 份，而
  `agent/inbox/spliced` 含同一文本 **24 条**、累计 **~31KB** ⇒ 违反 §2①「零信息重复必删」。
- **且它是那次真机会话故障的唯一载体**：注入消息的 `source` 是**承重字段**（`MessageSource` 类型必填；
  缺它时下游 `dsh-repeat-tool-reminder` 的 `agent/pre-step` 无保护地读 `message.source.kind`
  ⇒ TypeError ⇒ 整轮 `turn/end {kind:'error'}`）。⚠ **而 `systemPrompt.section` 从不创建消息
  ⇒ 结构上不可能触发它**。删掉注入路径**比给它补 `source` 更强**：补字段是修一个可能再次写错的点，
  删掉它让**整类错误无处发生**。

**为什么不与 0.11.0 的"观测层自我否决"冲突**：那里否决的是**订阅并记录活动事实**（信息纯可派生、
只能落 `runtime/` ⇒ 违反 I1/I3）。在场层**不记录任何事实**，它把**已可派生的**状态**注入上下文**。
**同一份信息，被消费 ≠ 被存储。**

**为什么不做成 `pre-execute` 拒绝闸**：治理在场的正确形态是**可见**，不是**阻断**——
未经登记的写入被当场拒 ⇒ agent 无法开发，直接违背"让 AI 更好地把控**和开发**"这一目标本身。
与 §10 同一判例：**代理指标做成闸门必然退化成狼来了**。

**机检守卫**：host 内**不得出现** `additionalContexts` / `createUserMessage` / `'tools/post-execute'`
（源码级断言 + 行为断言，见 `test/host.test.mjs`「在场层只有一条注入路径」）。

### 治理主权与活力（0.11.0 · 只读信号，**不是闸门**）

`nav_graph mode=health` 分三层报告：**覆盖**（登记率 / STALE）· **主权** · **活力**。

| 信号 | 判据 | 派生自 |
|---|---|---|
| **主权** | 项目是否在插件之外自建了治理（外来治理脚本 / 并行账本） | 纯磁盘扫描（`governanceSovereignty`） |
| **活力** | 最近一次改动是否晚于最近一次治理登记（"治理被绕过"） | 文件 `mtime` + 事件流（`governanceVitality`） |

**两条硬约束**：
1. **零新增状态**：两个信号全部纯派生（磁盘实况 + 事件流），**不订阅任何 Host 事件**。
   判据是结构性的——订阅产出只能落 `runtime/`（**可丢**，I3），而"能被丢掉的那份不可能是真相"（I1）。
   0.11.0 施工中曾计划加"观测层"，实测证明其信息量为零 ⇒ **取消**（GTP 落地文档 §2.4）。
2. **只报告，不拒绝**：主权/活力是**代理指标**，做成闸门必然退化成狼来了（与 §10 同一判例）。
   接管的正确形态是「登记 + 通报 + 收敛」；**删除外来治理件是危险操作，本插件不做**。

**opt-out（对标 Allstar 的 opt-out 策略）**：登记 `artifact` 节点即视为**已接管**，档位由 `when` 的**首词**决定：

| 首词 | 语义 | 是否继续告警 |
|---|---|---|
| `exempt` | 确认保留（理由在正文，可审计） | 否 |
| `refs` | **领域适应度函数**（管的是别的领域，非元治理重复） | 否（退出"未知外来件"） |
| `competing` | 确认与插件职能重叠的**第二本账** | **是**（升级为"已接管·待收敛"，带移交路径） |

⚠ **只认首词，不做全文匹配**（0.11.0 实测的判定冲突）：先前用全文正则时，某条 `when`
为了**说明移交路径**而提到 `exempt` 二字，该件就被同时算成 competing 与 exempt —— 两档语义打架、
结果取决于措辞。**声明字段必须机器可判，不能靠人读整句。**
⚠ **不做"登记即静音"**：`competing` 登记后仍告警。否则登记会让未收敛项伪装成已解决（假绿）。

**接管单元是入口，不是实现细节**：一个 `check-runner.mjs` 驱动 N 个 `check-*.mjs` 时，
**登记 1 个入口即覆盖它驱动的全部脚本**。逐个登记 91 个脚本是错误粒度（§2②：聚合须给出取回路径）。
⚠ `artifact` 的 `path` / `when` 落在节点**顶层**，不在 `meta.*`（0.11.0 实测：读 `meta.when` 恒空，
豁免永不生效且看起来像"功能已实现"）。

## 8. 不可丢弃的事故事实（F1–F9 · 是需求，不是历史）

| # | 事实 | 现架构如何满足 |
|---|---|---|
| F1 | 两会话并发写 → 索引被覆盖回退 | 事件流 append-only；模型缓存由 runtime 锁保护重写 |
| F2 | 破锁竞态 `stat→unlink` 删掉别人新锁 | 破锁用 `rename + token 校验`；锁必须可重入 |
| F3 | 假警报腐蚀信号 | 退役语义 + I1 单源 |
| F4 | scope 解析曾丢路径 ⇒ 指纹恒空 | 落点 = 索引键 ∪ 字面量 ∪ glob 展开，带断言，解析不出就明说 |
| F5 | 装与重启是两条时间线 | 发布链契约照旧（tgz → profile → 重启） |
| F6 | Windows 可写根只有一个、不含 `~/.dsh` | **宿主侧约束**（0.9.2 起不在本插件内满足） |
| F7 | 服务账户无 logon SID ⇒ 沙箱后端起不来 | **宿主侧约束**（同上） |
| F8 | 索引"能增不能删"⇒ 永久假 STALE | 退役能力保留 |
| F9 | 保存旧对象 ⇒ 内容静默不落盘 | 只用**追加**与**整体重写 + 读回校验** |

## 9. 文件落点

| 落点 | 职责 |
|---|---|
| `host/index.js` | Cordis 装配：**5 工具注册 + 在场层注入**（唯一 host 面） |
| `core/paths.js` · `lock.js` | 基础层：路径契约与平面 · runtime 锁（F2） |
| `core/log.js` | IO 层：事件流追加/读取/校验（F9）· **整体重写 + 读回校验（`rewriteVerified`，所有 runtime 文件的唯一写法）** |
| `core/scope.js` | IO 层：落点解析三来源（F4）+ 证据指纹 + **import 静态扫描与其扫描缓存** |
| `core/model.js` | 域层：折叠（I1）+ 磁盘实况（STALE / 缺口 / **依赖图**）+ **`foldOnly`（在场层的廉价折叠）** |
| `core/gates.js` | 查询层：七闸（纯函数） |
| `core/commit.js` · `render.js` | 编排层：写入编排 + 按证据收口 · **按需渲染（纯计算，零写盘）** |
| `core/format.js` | 表现层：**一切"说给模型看"的文本都在这里成形**（含**在场层** `renderPresence`） |
| `test/` | 领域行为 · 不变量 I1–I3 与 A1–A6 · 并发与锁 · host 装配面 |

**依赖方向**：`paths ← {log, lock} ← scope ← model ← gates ← commit ← host`。
> `scope → log`（0.11.0 引入）：扫描缓存要落 `runtime/`，而**一切 runtime 文件的写入方式只有一种**
> （`rewriteVerified`：整体重写 + 读回校验，F9）。把写盘方式复制一份到 `scope.js` 会立刻造出
> **第二种写法**——那是下一个"两处真相"。故宁可让 `scope` 依赖 `log`，也不许它自己写盘。
> 这是**方向收紧**（同层内单向），不引入环：`log` 不依赖 `scope`。
> `render.js`（0.12.0）：落盘面退场后它**零写盘**，故**不得** import `log`/`rewriteVerified`
> —— 写盘方式仍然只有一种，且写盘点只减不增。

新增文件 = 架构变更，须先改本文。测试断言数**不写在这里**（那是会漂移的数字，跑 `npm test` 即得）。
`runtime/` 下的文件名**不算文件新增**（它们是可丢弃缓存，见 §3）：新增一个 runtime 缓存只需登记进
`paths.js` 的 `RUNTIME_FILES`，不改本表的落点面——但**必须**回答"它是不是第二层里的磁盘实况衍生"，
答不上来即越层（§3「不存在第三层」）。

## 10. 文件职责压力（只读信号，不是闸门）

**为什么不设行数红线**：行数是**代理指标**。长文件未必坏，短文件照样能混三个职责；而一旦把行数做成闸门，
它必然退化成"狼来了"——与 0.10.0 修掉的计数闸退化（收口回执被当补丁计数、阈值 3 实际 ~1.5 就触发）是同一类错。

**判据是结构，不是长度**。`filePressure(model)`（`core/model.js`）派生三个量：

| 量 | 含义 | 由什么派生 |
|---|---|---|
| `ownerCount` | 这个文件被**几个不同架构节点**登记为落点 | 节点落点表（事件流） |
| `din` / `dout` | 被多少文件 import / 自己 import 多少文件 | 依赖图（磁盘 import 扫描） |
| `over` | `ownerCount ≥ 3` | 上述两者 |

`license`：零手写、删 `runtime/` 可无损重建（I1/I3）。它只经 `nav_graph mode=health` **报告**，**不参与七闸、不拒绝任何写入**——
拆不拆是架构判断（走 ADR），不是阈值判断。`0.10.2` 起可用；阈值 3 与计数闸同一量级。
