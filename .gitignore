# .internal 三层数据面（ARCHITECTURE.md §2）——**本仓不是治理根**，收敛为「一律忽略」
# 2026-09-12 判因：治理根是 D:\FF（事件流与模型快照在那边）；本仓此前的 PROJECT.md /
#   .internal/ARCH-MODEL.md / .internal/events.jsonl 是 0.9 自举期的历史快照（**无法被 nav_render 重算**，
#   且携带假事实），已随死投影清理删除 ⇒ 原先为它们保留的三条 `!` 强制入仓规则
#   （!.internal/、!.internal/events.jsonl、!.internal/ARCH-MODEL.md）以及 .internal/runtime|legacy 规则
#   全部失效。按「能删才不留假 STALE」收敛为单一忽略规则，不保留空承诺。
.internal/

# .roundtable/ —— 圆桌会议插件运行时数据（会议记录里 kbPath 是开会时选的**本机绝对路径**）
#   判据同 shoucang 仓（2026-09-16 已确立）：这类过程产物**永不入库**，靠 .gitignore 显式排除，
#   否则"未被跟踪"只是巧合 —— git add -A 就会带进去。
.roundtable/

node_modules/
*.tgz
.workbuddy/
.internal-bak*/
.dsh-vision-toolkit/
*.tgz.superseded*
# 本仓自举/临时产物
.npm-cache/
.migrate-sim/
.install-sim/
.verify-tmp/
.git-commit-msg.tmp
.git-msg*.tmp
